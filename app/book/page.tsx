"use client";import { useState } from 'react';export default function Book(){const [ok,setOk]=useState(false);function submit(e){e.preventDefault();setOk(true);}return(<main className='container' style={{padding:'32px 16px',maxWidth:560}}><h1>Book a Consultation</h1><form onSubmit={submit} style={{display:'grid',gap:12}}><input placeholder='Full name' required/><input type='email' placeholder='Email' required/><input type='tel' placeholder='Mobile (for SMS updates)' required/><input placeholder='Placement (e.g., forearm)'/><input placeholder='Approx size (inches)'/><textarea placeholder='Tell me about your piece' rows={5} required/><button className='btn btn-primary'>Request consult</button><p style={{fontSize:12,opacity:.7}}>By submitting you agree to receive SMS re: your appointment. Msg & data rates may apply. Reply STOP to opt out.</p></form>{ok && <p style={{marginTop:12,opacity:.8}}>Thanks — I’ll review and get back to you.</p>}</main>)}